generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["driverAdapters"]
}

datasource db {
  provider = "postgresql"
  url = env(env("NODE_ENV") == "development" ? "DATABASE_URL" : "DATABASE_URL_PROD")
}
